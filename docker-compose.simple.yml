services:
  korpsinventar:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - korpsinventar-data:/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - DB_PATH=/data/korpsinventar.db

volumes:
  korpsinventar-data:
